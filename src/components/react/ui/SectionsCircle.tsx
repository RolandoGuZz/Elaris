interface PropsCircle {
  title: string;
  position: number;
  active: boolean;
}

export const SectionsCircle = ({ title, position, active }: PropsCircle) => {
  return (
    <div>
      <p></p>
    </div>
  );
};
